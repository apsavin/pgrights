import Router from '../models/Router';
import DbConnectionsManager from '../models/DbConnectionsManager';

export const router = new Router();
export const dbConnectionsManager = new DbConnectionsManager();
